{
  "name": "kxta-core",
  "version": "2.0.4",
  "description": "Core context and command engines for Kontexta",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp -r src/db/migrations dist/db/ && mkdir -p dist/agent-rules && cp src/agent-rules/rules-block.md dist/agent-rules/rules-block.md",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "better-sqlite3": "^12.10.0",
    "chokidar": "^4.0.0",
    "glob": "^11.0.0",
    "jsdom": "^29.1.1",
    "simple-git": "^3.27.0",
    "turndown": "^7.2.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/jsdom": "^28.0.1",
    "@types/turndown": "^5.0.6",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "rulesVersion": "2.0.0"
}
